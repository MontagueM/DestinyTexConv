##########################
#   CHANGE VALUES HERE   #
##########################
unpacked_directory = 'C:/UnpackDirectory/Right/Here/'  # Must contain a full unpack, otherwise some textures may fail
output_directory = 'C:/MyOutput/Folder/'
header_file_name = 'ABCD-0123'  # If this string length != 9 it will fail
texture_format = 'DDS'  # only DDS and TGA are supported
